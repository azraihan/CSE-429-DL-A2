# =============================================================================
# File:     src/doc_agent/retrieval/__init__.py
# Layer:    Stage 5 - retrieval package marker
#
# Contains:
#   retriever.py  dense top-k search over the FAISS index, plus the evidence
#                 strength helpers (top_score / is_weak / next_k) that drive the
#                 agent's evidence-gated re-search
#   rerank.py     optional cross-encoder reranking of the candidate set (stub)
# =============================================================================

